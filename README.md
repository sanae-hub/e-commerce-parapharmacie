# 🚀 BACKEND STARTUP CORRECT Windows CMD\n\n## Checklist (cocher après chaque étape)\n\n- [ ] 1. PostgreSQL démarré\n  `docker run --name parapharmacie-pg -e POSTGRES_PASSWORD=pass -e POSTGRES_DB=parapharmacie -e POSTGRES_USER=user -p 5432:5432 -d postgres`\n  ou local + DATABASE_URL=postgresql://user:pass@localhost:5432/parapharmacie\n\n- [ ] 2. Backend deps\n  `cd backend ; npm install`\n\n- [ ] 3. Config DB\n  Éditer backend/.env → DATABASE_URL correct\n\n- [ ] 4. Sync DB + Seed\n  `cd backend ; npm run db:push ; npm run seed`\n\n- [ ] 5. Start Backend\n  `cd backend ; npm run dev`\n  → http://localhost:5000/api/health\n\n## Test Complet\n`cd backend ; npm run db:push ; npm run seed ; npm run dev`\n\n**Status: Prêt → Exécutez étape par étape !**
